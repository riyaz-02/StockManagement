# Nginx Configuration for Laltu Guinea Palace API
# File: /etc/nginx/sites-available/laltu-api

server {
    listen 80;
    server_name api.laltuguineapalace.in;

    # Redirect HTTP to HTTPS (will be configured after SSL setup)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # File upload size limit
    client_max_body_size 10M;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss;
}

# After SSL setup, this block will be auto-generated by Certbot
# server {
#     listen 443 ssl http2;
#     server_name api.laltuguineapalace.in;
#     
#     ssl_certificate /etc/letsencrypt/live/api.laltuguineapalace.in/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.laltuguineapalace.in/privkey.pem;
#     
#     # ... rest of configuration same as above
# }
